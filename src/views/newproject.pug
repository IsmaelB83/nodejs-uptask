extends layout

include mixins/formProyecto

block Contenido
    .contenido-principal
        h1 #{nombrePagina}
                
        if errores 
            each error in errores 
                .alerta.error= error.texto
        +formProyecto(project)

